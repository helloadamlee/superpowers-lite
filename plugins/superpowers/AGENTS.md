# Codex Plugin Development

This source is the Codex-only Superpowers plugin. Keep role routing deterministic,
preserve the MIT license, and never silently substitute a model or agent role.

Before changing shell scripts, run their focused tests and syntax checks. Before
claiming completion, run scripts/verify-codex-plugin.sh and report its output.
